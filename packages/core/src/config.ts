enum FieldKey {
  $value = '$value',
  $isDirty = '$isDirty',
  $isFocused = '$isFocused',
  $isTouched = '$isTouched',
  $errors = '$errors',

  name = 'name',

  set = 'set',
  change = 'change',
  blured = 'blured',
  focused = 'focused',
  changed = 'changed'
}

export { FieldKey };
