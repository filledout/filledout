enum FieldKey {
  // state
  $value = '$value',
  $isDirty = '$dirty',
  $errors = '$errors',
  $isTouched = '$touched',
  $isFocused = '$focused',

  set = 'set',
  change = 'change',
  blured = 'blured',
  changed = 'changed',
  focused = 'focused',

  name = 'name'
}

export { FieldKey };
