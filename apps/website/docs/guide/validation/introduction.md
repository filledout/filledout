# Validation

By default core version of form model have no validation in-built You should implement it manually by building flow from submit event to submitted/rejected events with validation inbetween.

There are few pre-built validation strategies using different schema validation libraries You can see short reference for them in the next sections.

